package Aula7.atividade;
/*Crie um programa em Java que simule um sistema bancário básico. O sistema deve ter as seguintes classes:
 Conta, Cliente e Banco.*/

//Classe Cliente: Atributos: nome, idade e CPF. Métodos: getters e setters para os atributos.

/* Classe Conta: Atributos: número da conta, saldo e cliente (representado por um objeto da classe Cliente).
Métodos: getters e setters para os atributos, além de um método depositar(double valor) que adiciona o valor ao
saldo da conta, e um método sacar(double valor) que realiza um saque, desde que o saldo seja suficiente.*/

/*Classe Banco: Atributo: uma lista de contas (deve ser um ArrayList). Métodos: criarConta(Cliente cliente), que cria
uma nova conta para o cliente informado e a adiciona à lista de contas do banco; buscarConta(int numeroConta), que retorna
a conta correspondente ao número informado; listarContas(), que exibe as informações de todas as contas do banco.*/

/*Crie uma classe Main para testar o sistema, onde você deve criar clientes, criar contas para esses clientes
e realizar algumas operações de depósito e saque.*/

public class Exercicios {
}
